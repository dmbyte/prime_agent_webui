# Prime Agent WebUI v0.5.32

v0.5.32 supersedes v0.5.31 with image identities generated using the deployed
Spark runner account's live UID/GID values. The installer's exact-identity gate
correctly rejected the v0.5.31 manifest before activating any image.

The functional change remains `prime-bmc-headless-browser` 1.1.1: Playwright
stays isolated in a worker subprocess, while thread-backed pipe I/O avoids the
persistent IPython child-watcher failure seen on a second browser launch.

The corrected candidate completed a forced cancellation followed by two clean
browser launches in IPython and navigated read-only to the actual iLO endpoint
with HTTP 200. The release suite passes all 113 dashboard tests and four managed
skill tests.
