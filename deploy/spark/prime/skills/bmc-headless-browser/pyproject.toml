[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
# Thread-backed worker I/O stays reliable across repeated launches in Prime's persistent kernel.
name = "prime-bmc-headless-browser"
version = "1.1.1"
description = "Prime/OpenShell browser adapter for BMC web interfaces"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/bmc_headless_browser"]
