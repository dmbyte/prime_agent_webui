# v0028 — OpenAI GPT-5.4 configured

Date: 2026-08-23

The rotated user-supplied key is loaded into `prime-web.service` from a mode-0600
environment file outside the repository. Prime enables `openai/gpt-5.4`; the
Parameters selector and Usage row now represent it accurately as configured.

Secret-safe checks confirmed the service received the credential variable and
Prime enabled GPT-5.4. A minimal request reached OpenAI but returned `no credits
remaining`, confirming configuration/authentication while establishing that API
billing credits are the remaining blocker. Both local model gates still pass.

No secret is recorded here or in Git. Rollback removes the environment-file link
and enabled model and restores v0027 dashboard files. This snapshot is immutable.
