# v0024 — System monitor metric order

Date: 2026-08-23

The monitor's first row now shows CPU, GPU, Memory, and Power. Its second row
shows CPU temp, GPU temp, and System temp. JavaScript syntax passed and the live
browser confirmed the exact requested order.

Rollback restores the v0023 dashboard JavaScript. This snapshot is immutable.
