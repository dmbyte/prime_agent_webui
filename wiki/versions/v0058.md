# v0058 — Inventory running browser interfaces

Date: 2026-08-25

Prime WebUI is the only browser interface reachable from the LAN/VPN, on private
HTTPS port 8443. Hermes WebUI (`127.0.0.1:8787`), NVIDIA DGX Dashboard
(`127.0.0.1:11000`), and CUPS (`127.0.0.1:631`) are active but loopback-only.
ttyd (`127.0.0.1:7681`) is available only as Prime's authenticated Advanced
console route rather than as an independent LAN listener.

The auth broker/API on 8764/8765, vLLM endpoints on 30000/30001, and dynamic
Prime kernel-worker ports are not independent WebUIs. This inventory was
verified from listeners, services, containers, Nginx routes, HTTP responses,
page titles, and process command lines. No configuration changed. This snapshot
is immutable.
