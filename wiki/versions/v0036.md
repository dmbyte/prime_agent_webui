# v0036 — Browser session attachment retired

Date: 2026-08-24

The later of two ttyd clients, PID 3111439, was identified as the extra attachment
created during live-console testing and terminated. The original main terminal
client, PID 3111303, remained in place, and ttyd was not restarted. The
daemon-managed running job was not stopped.

The activity overlay is now event-feed-only. The old live-console control is
hidden for cached-code compatibility, its stylesheet was removed, and the browser
launcher rejects every `--attach` request with exit 64 before Prime can launch.
The main terminal and dashboard services remained active, explicit attach rejection
passed, and full Spark validation passed. Restoring v0035 attachment behavior is
not recommended without disposable-task testing. This snapshot is immutable.
