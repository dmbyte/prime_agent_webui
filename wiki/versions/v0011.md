# v0011 — Private LAN/VPN Prime browser access

Date: 2026-08-23

Extended the PAM-authenticated Nginx endpoint to the Spark's specific LAN address
at 172.16.253.231:8443. Nginx accepts only loopback, RFC1918, and
100.64.0.0/10 source addresses and denies all others. The ttyd backend and both
model APIs remain loopback-only. No public tunnel or router forwarding was added.

Rotated the self-signed certificate to include the LAN IP and preserved the
v0009 certificate/key in a root-only archive. Nginx validation, Spark-local and
Mac-to-LAN 401 challenges, service state, model health, private bindings, and the
memory gate passed. The obsolete client SSH tunnel was stopped.

Rollback restores the v0009 Nginx site and archived certificate. This snapshot
is immutable.
