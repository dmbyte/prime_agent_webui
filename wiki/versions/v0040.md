# v0040 — Recoverable right-click conversation deletion

Date: 2026-08-24

Conversation rows now open a custom menu on right-click with **Delete
conversation**. After confirmation, the API validates the exact session ID,
refuses live sessions, and atomically moves an inactive transcript into private
mode-0700 recovery storage. It does not permanently unlink the file.

Deleted conversations leave the sidebar, while Usage continues scanning recovery
storage so historical token and spend totals remain intact. Python and JavaScript
checks passed; the deployed endpoint rejected a traversal-style identifier with
HTTP 400, the existing 40-row catalog remained intact, UI assets were present, and
full Spark validation passed. No genuine conversation was deleted during testing.
Rollback restores v0039 UI/API assets; moved transcripts remain recoverable. This
snapshot is immutable.
