# v0013 — Nginx PAM account retrieval enabled

Date: 2026-08-23

Following explicit informed approval, added `www-data` to supplementary group
`shadow` (GID 42) and restarted Nginx. All inspected workers inherited GIDs 33
and 42. This resolves the Ubuntu PAM account-retrieval failure responsible for
the repeating browser credential prompt.

The unauthenticated endpoint still returns the required 401 challenge. Nginx
syntax and the complete model/browser/private-binding/memory gate pass. Positive
login awaits the user's interactive retry because the password was never shared.

Security consequence: compromise of Nginx can expose system password hashes to
offline attack. Rollback removes `www-data` from `shadow` and restarts Nginx,
which disables this PAM authentication path. This snapshot is immutable.
