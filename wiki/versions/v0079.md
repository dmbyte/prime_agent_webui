# Version v0079 — Staged rootless task execution

Date: 2026-08-26
Supersedes: [v0078](v0078.md)

## State captured

- The Spark has rootless Podman 4.9.3 and prerequisite packages installed.
- The general Prime task candidate image runs Prime 0.8.0 and has immutable
  digest `sha256:e6a7e19dafb90cc737775e0bca74e22e2e1e680853061142058616d30664eddf`.
- The repository has server-side task authorization, composer confirmation
  controls, and a fail-closed rootless runner with per-user storage and hard
  resource/network boundaries.
- Prime's official v0.8.0 artifact SHA-256 is pinned; public npm installation was
  proven unavailable and the older updater strategy is classified as defective.
- JavaScript syntax validation and all 39 local tests pass.
- RPC reads are bounded; startup failures are cleaned up; stop/steering writes no
  longer hold the global task lock; and steering/follow-up waits for a Prime RPC
  acknowledgement. The tracked Agent updater now uses the official release
  artifact and published checksum rather than the unavailable npm registry name.
- Production WebUI task execution remains on v0078. Container mode is disabled
  until the gateway, separate identity, per-user migration, remaining images, and
  end-to-end gates are complete.
- The policy controls and RPC stability fixes are deployed in host-compatible
  mode. Auth/API are active, unauthenticated HTTPS API access returns 401, and
  the container feature flag is absent.

## Recovery

The root-only pre-change bundle is
`/var/backups/prime-containers-v0079-20260826T145311-0500` and contains 38 verified
checksums. It predates Podman installation. To return the package layer as well,
remove only the explicitly installed Podman/rootless packages after confirming no
other workloads use them. Never edit this snapshot; record any rollback as a new
version.
