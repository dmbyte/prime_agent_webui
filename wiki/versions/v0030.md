# v0030 — Configured provider usage groups

Date: 2026-08-24

Usage now renders only configured/authenticated models. Multi-model providers are
collapsed by default into a provider summary that rolls up tokens and spend for
Today and Last 30 days. Expanding the provider reveals individual model rows;
single-model providers remain direct rows.

The live browser showed `openai-codex · 13 models` as a collapsed roll-up, hid
GPT-5.6 Sol until expansion, and retained direct OpenAI, Nemotron, and Qwen rows.
Rollback restores v0029 dashboard HTML, JavaScript, and CSS. This snapshot is
immutable.
