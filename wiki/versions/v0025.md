# v0025 — Usage model availability audit

Date: 2026-08-23

Usage rows are generated from recorded assistant calls, not from the configured
model catalog. The audit found 214 Nemotron calls totaling 6,098,750 tokens and
$0 recorded cost. Qwen is enabled and its inference service is active, but it has
no recorded assistant calls and therefore no Usage row.

OpenAI is not configured as a Prime provider and no OpenAI API credential is
present in the inspected service environment, so it also has no Usage row. No
runtime or interface configuration changed. This snapshot is immutable.
