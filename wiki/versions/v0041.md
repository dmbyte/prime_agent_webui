# v0041 — Exact row targeting for conversation deletion

Date: 2026-08-24

Every rendered conversation row now carries its exact full session ID. The
right-click context menu resolves its delete target directly from that row, and
ordinary selection uses the same binding. Position-based array lookup has been
removed, so choosing the third row cannot silently resolve to the first row.

JavaScript syntax and deployed-code assertions passed. Both dashboard services
and the full Spark validation gate passed; no conversation was deleted during
testing. Rollback restores the v0040 dashboard JavaScript. This snapshot is
immutable.
