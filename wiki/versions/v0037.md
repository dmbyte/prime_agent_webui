# v0037 — Explicit attachment, event-only UI

Date: 2026-08-24

Session attachment is available again, correcting v0036's over-broad disablement.
The launcher requires the exact guarded form `--attach ID --explicit`, validates
the existing session, and then invokes Prime's native attach. Old two-argument
attach requests fail closed and cannot start a fallback conversation.

The dashboard JavaScript now contains no attach command, attach URL, live-console
control, or iframe. After the guard was installed, the stale reconnecting client
PID 3149324 was terminated and did not return; only the main terminal client PID
3113237 persisted. Deployed-code inspection, stale-form rejection, service health,
and full Spark validation passed. Rollback can restore the v0036 launcher while
retaining this event-only UI. This snapshot is immutable.
