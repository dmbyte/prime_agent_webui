# Version v0073 — Semantic security-review guardrails

Date: 2026-08-26
Supersedes: [v0072](v0072.md)

## State captured

- Machine-readable Prime events verified the forward test used local Qwen 3.6
  35B A3B at high effort with tools disabled.
- The test found the proxy-header trust area but used the wrong external attack
  path and emitted common semantic false positives.
- The skill now distinguishes explicit proxy header replacement from direct local
  backend bypass, valid double-submit CSRF from missing-HMAC claims, argv/`--`
  handling from downstream agent authority, guarded imports from live listeners,
  and CSP same-origin resources from inline scripts.
- Local gateway reachability and shared execution identity must each resolve to a
  finding or an evidence-based negative result.
- Output is required to be decision-dense rather than exhaustively speculative.

## Recovery

Restore the v0072 `SKILL.md` from commit `803cd32` if the new guardrails suppress
a valid finding, then document the rollback as a later snapshot. The capability
reference and WebUI runtime remain unchanged.
