# v0065 — Resolve Prime storage and internal conversation IDs

Date: 2026-08-25

Conversation deletion, opening, and export now resolve both Prime's transcript
filename ID and the internal session ID stored in the JSONL. The Spark currently
has 9 mismatches among 30 transcript files; those sidebar rows previously
produced false “Conversation no longer exists” errors.

Recovery moves retain the real storage filename so restoration remains exact.
The active-work guard protects both aliases when Prime reports actual running,
streaming, compacting, attached, queued, or unfinished work. Strict ID validation,
symlink rejection, the immediately-before-move activity recheck, private recovery
permissions, and auditing remain intact.

Two identifier regressions were added. All 21 local dashboard/auth/security tests,
Python/JavaScript syntax, and whitespace checks passed. Deployment validation and
the checksummed pre-change bundle
`/var/backups/prime-session-ids-v0065-20260825T175500-0500` are recorded in the
current-state and dashboard pages. This snapshot is immutable.
