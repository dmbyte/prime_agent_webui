# v0026 — Zero-usage model rows and OpenAI re-audit

Date: 2026-08-23

Usage now unions the configured/intended model catalog with recorded activity.
Nemotron, Qwen, and planned OpenAI GPT-5.6 Sol therefore always appear. Qwen is
configured and currently displays zero usage. OpenAI displays zero and `not
configured`.

A secret-safe audit found Prime's `auth.json` empty, only the two Spark providers
in its model catalog, and no OpenAI-style key outside conversation history. Prime's
earlier claim that it configured the supplied key was not borne out by persisted
state. The historical key must not be recovered or reused; rotate it first.

The API and live browser showed all three rows, and both local models/full Spark
gate passed. Rollback restores v0025 API and JavaScript. This snapshot is immutable.
