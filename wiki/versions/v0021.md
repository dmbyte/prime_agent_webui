# v0021 — GitHub repository renamed

Date: 2026-08-23

The private GitHub repository is now
`https://github.com/dmbyte/prime_agent_webui`. The local `origin` fetch and push
URLs use the renamed repository. GitHub reports private visibility and `main` as
the default branch.

Historical v0020 references preserve the repository name that was true at that
snapshot. Rollback renames the repository to `dgx-spark` and updates `origin`.
This snapshot is immutable.
