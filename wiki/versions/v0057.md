# v0057 — Validate the dedicated WebUI credential

Date: 2026-08-25

The owner created the dedicated WebUI password interactively with the no-echo
password tool. Automation did not receive the password and did not inspect the
contents of its one-way salted-scrypt record.

The credential file is mode 0600 and owned by `dbyte` inside a mode-0700 owner
directory. The loopback broker reports the credential configured, and the
owner's browser login returned HTTP 200. The authentication service is active,
Nginx validation passes, and Fail2ban has no currently banned source. This
snapshot is immutable.
