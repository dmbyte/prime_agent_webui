# State Snapshot v0076

Date: 2026-08-26
Supersedes: [v0075](v0075.md)

## State

- Immediate user-message echo and bounded live dialogue progress remain enabled.
- The composer offers Message, `/steer`, and `/stop` while work is active.
- Steering uses Prime 0.8.0's parser-verified
  `send <agent> --message <text> --json` form; sends to busy agents are steering.
- The unsupported follow-up choice is absent because the installed parser rejects
  the delivery flags still shown by generated help.
- Owner, running-state, ID, length, timeout, and argument-array controls remain in
  force.

## Recovery

The root-only pre-fix bundle is
`/var/backups/prime-steering-v0076-20260826T135500-0500`. The broader pre-feature
bundle remains `/var/backups/prime-live-dialogue-v0075-20260826T134500-0500`.
