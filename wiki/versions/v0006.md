# Version v0006 — Verified Spark pre-change baseline

- Date: 2026-08-23
- Status at capture: current
- Previous version: `v0005`

## State captured

The target DGX Spark was inventoried before Prime implementation. It already runs
the selected Nemotron 3.5 NVFP4 target with DSpark under vLLM 0.27.1, using a
65,536-token context, 24 GiB KV cache, and 0.78 memory-utilization setting. An
older Hermes/WebUI installation is enabled and active. No Prime deployment or
Qwen3.6 specialist exists yet.

A root-only configuration recovery artifact and detailed manifests reside at:

`/var/backups/dgx-spark-baseline-20260823T154649-0500`

Archive SHA-256:
`dbac814e640e41293ffc589b5ece0489d7f514e81948b078d227dc66b7fba953`

All recorded checksums passed. Existing services remained running. Detailed
verified facts and recovery limits are in `wiki/SPARK_BASELINE.md`.

## Restoration notes

This snapshot documents state; the protected tar should be restored selectively,
not extracted blindly over a live host. It is a configuration snapshot rather
than a disk image. Stop and validate affected services around any restoration.

## Known gaps

- The recovery artifact remains on the same physical NVMe as the live system.
- User data, model weights, session history, logs, and external state are not
  backed up by this snapshot.
- Hermes WebUI is process-active but failed the direct HTTP health probe.
- LAN-facing ports and inactive host firewall require review before sensitive
  credentials or financial tools are added.

