# Version v0175 — Preserve Codex OAuth rotation

Date: 2026-09-24

## Verified state

- The failed task selected `openai-codex/gpt-5.6-sol` and failed at its first
  model call, while both local model endpoints returned HTTP 200.
- The gateway credential expired on 2026-09-03. A safe refresh probe returned
  HTTP 401 with code `refresh_token_reused`; no token value was displayed.
- The installer now copies a host Codex credential only when its expiry is
  strictly newer than the gateway copy.
- The deployed gateway reports an actionable, sanitized `/login` message and
  records the safe `codex_credential_error` category in journald.
- Explicit slash-model directives resolve before phrase rules. The preserved
  request was restarted and visibly routed to Qwen's 98,304-token context.
- All 121 dashboard tests and complete release validation pass.

## Rollback

Restore v0.5.39. This restores unconditional credential overwrite and ambiguous
slash-directive routing.
