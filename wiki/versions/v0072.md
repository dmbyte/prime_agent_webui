# Version v0072 — Forward-tested security-review refinements

Date: 2026-08-26
Supersedes: [v0071](v0071.md)

## State captured

- A tools-disabled forward test showed that the generic skill eliminated the
  prior CSP and Origin-forwarding false positives and improved capability-based
  negative, subprocess, and resource-lifecycle analysis.
- The initial skill still missed local gateway assertion forgery and the tenant
  consequences of shared execution identity, and its narrative invented runtime
  model metadata.
- The skill now explicitly requires direct-backend reachability and proxy-
  assertion tests for local/sibling processes, containers, plugins, jobs, and
  agents.
- Every tenant boundary must be compared against the OS identity, filesystem,
  credentials, tools, terminals, caches, and IPC actually used by background work.
- Provider, model, effort, and tool metadata may be stated only from trusted
  runtime metadata; otherwise they must be reported as unknown.

## Recovery

Restore the two v0071 skill files from commit `5f8ab78`, or from the root-only
pre-install bundle documented in current state, then record the rollback as a new
snapshot. No WebUI runtime or security control changed in this version.
