# v0020 — Private GitHub backup published

Date: 2026-08-23

The project is now versioned on Git `main` and published to the private GitHub
repository `https://github.com/dmbyte/dgx-spark`. The initial commit contains the
complete reviewable Spark deployment source, interface, operating documentation,
decision history, and immutable wiki snapshots. `main` tracks `origin/main`.

Repository exclusions cover caches, environment files, logs, certificates, and
private keys; the pre-commit credential-pattern scan found no matching files.
Rollback of the remote would require explicit deletion confirmation. The local
Git history remains independently recoverable. This snapshot is immutable.
