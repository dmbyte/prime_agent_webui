# Version v0070 — Release-aware Prime updates

Date: 2026-08-25
Supersedes: [v0069](v0069.md)

## State captured

- The first Prime WebUI GitHub release is displayed as `.1`, tagged `v0.1.0`,
  and points to commit `5d9fd3a`.
- Entering Settings checks published releases for both Prime Agent and Prime
  WebUI, displays installed/latest state, and makes an available update prominent.
- Prime Agent updates install the exact npm version matching the validated
  official release tag.
- Prime WebUI updates fetch and fast-forward to the exact validated private
  release commit instead of tracking `origin/main` HEAD.
- Settings uses the same grouped card hierarchy and visual language as Admin.
- Numeric semantic-version comparison has regression coverage; the complete
  local suite contains 28 tests.

## Recovery

The prior deployed repository state is commit `5d9fd3a`. A root-only checksummed
copy of the prior update/API/UI artifacts and version state is on the Spark at:

`/var/backups/prime-releases-v0070-20260825T204000-0500`

Prefer restoring the affected tracked file or returning the clone to the prior
commit. Never edit this snapshot; document any rollback as a later version.
