# v0039 — Attach artifacts hidden from Conversations

Date: 2026-08-24

The dashboard API now omits saved sessions whose sanitized first user topic is
exactly `attach`. Filtering occurs before the 40-row limit, allowing genuine older
conversations to fill all available positions. The eight identified artifact files
remain untouched for recovery and audit, and usage accounting is unchanged.

The deployed API returned 40 visible conversations with zero exact `attach` topics.
The dashboard service and full Spark validation passed. Rollback restores the
v0038 API and makes the retained artifacts visible again. This snapshot is
immutable.
