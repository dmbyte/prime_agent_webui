# v0049 — Correct the Pi 5 power-button end

Date: 2026-08-24

The owner corrected the physical location of the Raspberry Pi 5 native power
button and noted the LED immediately beside it. The former model incorrectly put
the actuator near the middle of the USB-C/HDMI side.

The actuator now passes through the short microSD end near the USB-C corner. A
separate 2.9 mm sight hole exposes the adjacent bi-colour status LED. Button Y,
LED Y, and shared Z positions are top-level fit parameters, and the generated
validation report records them. The separate plunger is unchanged as a printable
part but is rotated through the short end during assembly.

The base, lid, and actuator were regenerated and checked as single-body,
watertight, winding-consistent positive-volume meshes. X2D/PETG slice validation
passed with the left/main nozzle, no supports, warnings, or filament changes, and
a 2 h 29 m 48 s estimate; temporary G-code was discarded. A short physical fit
coupon remains required because Raspberry Pi labels its component geometry
approximate. ADR-0037 records the placement decision. This snapshot is immutable.
