# v0062 — Repair recoverable conversation deletion

Date: 2026-08-25

Conversation deletion now distinguishes Prime's persistent `lifecycle: live`
label from actual running activity. Idle, unattached conversations can be moved
to private recovery storage; sessions that are active, streaming, compacting,
attached, queued, or have unfinished work remain protected.

The right-click delete path clears the conversation view when its open chat is
deleted and displays API failures instead of failing silently. Bulk deletion
continues through selected chats, retains only failed selections, and reports
the individual failures.

Three deletion/activity regressions were added. All 17 local dashboard/auth/
security tests, JavaScript syntax, Python compilation, and whitespace checks
passed. Fourteen applicable deployed tests, actual Spark activity classification,
dashboard service state, installed asset markers, and the private HTTPS boundary
passed. The prior source and installed assets are preserved in the root-only
checksummed backup `/var/backups/prime-delete-v0062-20260825T174100-0500`.
This snapshot is immutable.
