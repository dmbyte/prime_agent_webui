# v0023 — Combined per-model usage

Date: 2026-08-23

The separate Tokens and Spend tabs are replaced by one Usage screen. Each exact
provider/model row displays token volume and recorded spend for the Spark's local
calendar day and a rolling 30-day period.

The API returned the expected Today/30-day periods and provider/model grouping.
The live browser showed the Model, Today, and Last 30 days columns with tokens and
spend in each row; complete Spark validation passed. Rollback restores v0022 API,
HTML, and JavaScript and removes `usage.css`. This snapshot is immutable.
