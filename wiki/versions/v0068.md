# v0068 — Refine Admin layout and active rename

Date: 2026-08-25

Live visual inspection found that generic flex styles made Admin labels and
values run together, compressed the retention explanation into a narrow column,
and crowded five user actions into one row. Admin now has clear System,
Maintenance, and WebUI-user sections, explicit status/role/state badges, bounded
retention input sizing, and responsive grouped user actions. Controls prohibited
for the current or initial administrator are visibly disabled with explanations.

A saved active conversation now has a Rename button in its header beside the
advanced console. New conversations hide the action. Successful renames update
the active title and refresh the conversation list.

All 25 local tests plus JavaScript syntax and whitespace checks passed before
deployment. Roll back to Git commit `f43f263` for the prior UI while preserving
all user data. This snapshot is immutable.
