# v0067 — Complete recoverable user-data cleanup

Date: 2026-08-25

Per-user cache deletion now moves chats, uploads, persisted task ownership and
logs, and owned usage-ledger records to private recovery storage. Persisting task
ownership makes cleanup complete after an API restart. Cache deletion also
revokes the affected user's browser sessions.

The Admin UI separates role changes from account enable/disable, and both revoke
existing sessions. Account creation and password reset use masked password fields
with browser validation instead of visible prompts. The initial administrator
and last-enabled-admin protections remain unchanged.

All 25 local tests and JavaScript syntax checks passed before deployment. The
pre-v0066 single-user rollback bundle remains root-only at
`/var/backups/prime-users-v0066-20260825T180700-0500`. This snapshot is immutable.
