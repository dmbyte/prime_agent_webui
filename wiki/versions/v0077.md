# State Snapshot v0077

Date: 2026-08-26
Supersedes: [v0076](v0076.md)

## State

- New submissions render immediately in the dialogue with delivery status.
- Native WebUI tasks use Prime 0.8.0 RPC with private piped stdin/stdout.
- Safe lifecycle, tool, elapsed-time, and draft-response progress renders live;
  hidden reasoning and RPC bookkeeping do not enter browser snapshots.
- Message, `/steer`, `/follow-up`, and `/stop` are available. They map to Prime's
  RPC `prompt`, `steer`, `follow_up`, and `abort` commands and remain owner-scoped.
- Task logs, progress events, response bookkeeping, and browser payloads are
  bounded.

## Recovery

The root-only pre-change bundle is
`/var/backups/prime-rpc-v0077-20260826T140000-0500`. The earlier full pre-feature
bundle remains `/var/backups/prime-live-dialogue-v0075-20260826T134500-0500`.
