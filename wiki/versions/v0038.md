# v0038 — State-aware overlay minimize control

Date: 2026-08-24

The activity overlay header now shows a minus while expanded and a plus while
minimized, clearly indicating the action available in each state. The tooltip,
accessible label, and `aria-expanded` attribute stay synchronized with the symbol.

JavaScript syntax and deployed-code checks passed. Both dashboard services and the
full Spark validation gate passed. Rollback restores the v0037 dashboard
JavaScript. This snapshot is immutable.
