# v0035 — Stop control in the main conversation

Date: 2026-08-24

The activity overlay no longer displays Stop task. A compact bar above the main
Prime terminal appears whenever tasks are active and contains the single visible
Stop task button. It tracks an active conversation selected from the sidebar and
offers a running-task selector when multiple conversations are working.

The confirmation prompt, strict current-active-ID validation, Prime single-agent
stop command, and saved-conversation retention are unchanged. JavaScript syntax,
deployed asset placement, and full Spark validation passed. Rollback restores
v0034 dashboard assets and removes `conversation-control.css`. This snapshot is
immutable.
